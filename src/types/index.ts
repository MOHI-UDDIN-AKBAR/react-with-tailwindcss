export type NavLinksType = {
  href: string;
  label: string;
};

export type Statistic = {
  value: string;
  label: string;
};

export type ShoeType = {
  thumbnail: string;
  bigShoe: string;
};

export type Product = {
  imgURL: string;
  name: string;
  price: string;
};
